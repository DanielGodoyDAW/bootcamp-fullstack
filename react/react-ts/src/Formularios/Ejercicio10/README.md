ex10 — Detectar problemas

Explica qué problema hay en cada caso y propón una corrección.

Caso A

```tsx
<input type="text" value={name} />
```

Caso B

```tsx
function handleSubmit(event) {
console.log("enviado");
}
```
Caso C

```tsx
<input
type="checkbox"
value={accepted}
onChange={(event) => setAccepted(event.target.value)}
/>
```

Caso D

```tsx
<label for="username">Usuario</label>
```

Caso E

```tsx
<button>Enviar</button>
```

dentro de un formulario, sin revisar qué función concreta debe cumplir.

Entrega

    Explicación breve por cada caso.
    Corrección propuesta.